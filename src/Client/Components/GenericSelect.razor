@typeparam TItem
<select>
    @foreach (TItem item in Items ?? Array.Empty<TItem>())
    {
        <option>@Content(item)</option>
    }
</select>
@code
{
    [Parameter]
    public IEnumerable<TItem> Items { get; set; }

    [Parameter]
    public RenderFragment<TItem> Content { get; set; }
}